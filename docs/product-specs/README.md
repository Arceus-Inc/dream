# Product specs

Human-authored product and API specs that outlive a single task. SDK-facing behaviour should
be reflected in `consumer-facing-api/` and pinned by tests; this folder holds longer-lived
design notes.
